# macOS-specific Ghostty Configuration

# Load base config
config-file = base.conf

# Font settings for macOS (Retina displays need larger sizes)
font-style = Medium
font-size = 12
font-thicken = true

# macOS-specific window settings
macos-titlebar-style = transparent
macos-option-as-alt = true
